#include <iostream>
using namespace std;
int main()
{
    ;this is the program of assembley language for adding two variable
    int a, b, sum;
    ;hello world
    cout << "Enter the two numbers you want to add: ";
    cin >> a >> b;
    ;world execution
    sum = (a + b);
    cout << "The addition of " << a << " and " << b << "are: " << sum;
    
    ;this is the program of assembley language for adding two variable
    int a, b, sum;
    ;hello world
    cout << "Enter the two numbers you want to add: ";
    cin >> a >> b;
    ;world execution
    sum = (a + b);
    cout << "The addition of " << a << " and " << b << "are: " << sum;
    ;this is the program of assembley language for adding two variable
    int a, b, sum;
    ;hello world
    cout << "Enter the two numbers you want to add: ";
    cin >> a >> b;
    ;world execution
    sum = (a + b);
    cout << "The addition of " << a << " and " << b << "are: " << sum;
    return 0;
}
